# Legacy modules

This folder keeps archived pages and components that are not part of the routed application. The build excludes `src/legacy/**/*` (see `tsconfig.app.json`) to avoid type-checking these files. Move items back to `src/` when reactivating a feature.
