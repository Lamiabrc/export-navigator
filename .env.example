# Mistral LLM (primary)
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-latest

# OpenAI LLM (fallback)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# SMTP (contact)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_SECURE=false
